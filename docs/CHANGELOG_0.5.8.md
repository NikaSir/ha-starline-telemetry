# 0.5.8

- Centre both vehicle images horizontally by their transparent asset bounds.
- Raise and vertically align the 130 and 683 scenes despite their different aspect ratios.
- Centre the armed/alarm field on the same anchor as the vehicle.
- Read current `arm` state from the official read-only StarLine device-data endpoint with a 60-second cache.
- Make the Header refresh button force a current read-only state refresh.
- Allow long metric labels to wrap without reducing typography.
- Preserve history, statistics, navigation and zoom behavior.
