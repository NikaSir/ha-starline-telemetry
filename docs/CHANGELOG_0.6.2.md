# StarLine 0.6.2

- Requires the one-shot source route and its timestamp as one complete pair, rejects invalid, stale and future timestamps, and consumes both values before applying return-route precedence.
- Adopts NikaS Specialized Panel UI Standard v1.9, Navigation Contract v1.1, explicit data-truth metadata and deterministic autonomous-bundle verification.
- Preserves the read-only registry-backed telemetry model, stable shell, vehicle geometry and current route `/starline`.
