# StarLine 0.6.3

- Corrects the visibly flattened 130-й artwork with a documented 1.05 vertical silhouette factor while retaining the common 72% width.
- Keeps both vehicles centred on the same visible-alpha centre and wheel line; every packaged engine, door, hood and trunk state uses the same per-vehicle correction.
- Gives operational values the full width of their cell on the second line so `Остановлен` and event text are no longer clipped.
- Preserves the approved font floors, 74 px status rows, iPhone safe areas, fixed Header and Bottom Tab Bar, one transform canvas and read-only telemetry behavior.
- Aligns the machine-readable StarLine contract with the already adopted NikaS UI Standard v1.9 and Navigation Contract v1.1.
