# 0.5.5

- Added state-aware vehicle images for engine running, open doors, open hood and open trunk for both cars.
- Added independent static armed/alarm fields and unavailable desaturation without flashing animation.
- Replaced the ambiguous security fallback `Норма` with `Нет данных` when no armed-state entity is available.
- Expanded the operational row so engine and event values can remain readable without reducing typography.
- History and statistics behavior are unchanged.
