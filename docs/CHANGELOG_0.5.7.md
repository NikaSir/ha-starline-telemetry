# 0.5.7

- Restore the Home Assistant core StarLine `lock` entity as a valid `arm`/security-mode source.
- Resolve all explicit security sources and prefer an active armed signal over a conflicting inactive signal.
- Move both vehicle images 86–88 px upward relative to 0.5.6.
- Move and enlarge the armed/alarm field with the vehicles.
- Preserve vehicle size, typography, zoom, history and statistics behavior.
