# StarLine 0.6.4

- Uses the visible proportions of each 683-й state as the direct rendering reference for the matching 130-й state instead of applying an approximate height multiplier.
- Preserves the common 72% visible width, horizontal alpha-frame centre and wheel line for both vehicles and every packaged state image.
- Gives the engine exactly one third of the operational row and keeps its `Двигатель` label on one line.
- Aligns each operational icon beside a two-line label/value text block so `Остановлен`, the latest event and `Снят` no longer drift underneath their icons.
- Preserves the approved typography floors, 74 px status rows, fixed shell, safe areas, transform canvas and read-only telemetry behavior.
