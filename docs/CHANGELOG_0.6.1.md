# 0.6.1

- Replaced per-vehicle CSS image-box percentages with one visible-alpha geometry model using meaningful-opacity bounds rather than faint shadow pixels.
- Normalized both vehicles and every packaged state scene to a common 72% visible width.
- Anchored visible vehicle bounds to one horizontal centre and one wheel line, independent of transparent canvas padding.
- Removed the open-state translation so doors, hood and trunk no longer move the entire vehicle layer.
- Fixed both status rows at 74 px so event text cannot change the photo-scene geometry.
- Restored the iPhone top safe area inside the fixed Header and made the Summary card consume only the remaining shell grid viewport.
- Replaced metric and status ellipses with compact internal spacing and up to two value lines without reducing typography floors.
- Softened the security arc and faded its lower ends so the open dome no longer reads as two vertical posts.
- Preserved the open-base security field, typography floors, fixed shell, zoom architecture and read-only data behavior.
