# 0.5.3

- Move the GSM/GPS connection chip into the free left portion of each Summary photo scene so it no longer covers the vehicle.
- Divide unused Summary viewport height evenly between the two photo scenes while retaining the fixed state-row and metric-plaque geometry.
- Show an armed vehicle explicitly as `Охрана / Включена` with a blue-tinted shield plaque; keep disarmed neutral and alarm red.
- Recalculate scene fit on viewport changes without changing typography floors, gesture scaling or native scrolling.
- Leave history retrieval, event timestamps, action labels and statistics unchanged.
