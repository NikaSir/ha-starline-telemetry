# 0.5.6

- The armed state now comes from StarLine security telemetry and never from the central-lock entity.
- Alarm keeps priority over the ordinary armed field; missing security telemetry remains `Нет данных`.
- Both cars are larger, lower and closer to the visual centre of the free scene area.
- The same per-vehicle geometry applies to default, engine, door, hood and trunk variants.
- Typography, scaling, history and statistics behavior are unchanged.
